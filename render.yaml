services:
  - type: web
    name: rut-verifier-api
    runtime: docker
    dockerContext: ./backend
    dockerfilePath: ./backend/Dockerfile
    plan: free
    envVars:
      - key: DIAN_DOCUMENT
        sync: false
      - key: DIAN_PASSWORD
        sync: false
      - key: PLAYWRIGHT_HEADLESS
        value: "true"
      - key: FRONTEND_URL
        value: https://rut-verifier-frontend.onrender.com
    healthCheckPath: /api/v1/health

  - type: web
    name: rut-verifier-frontend
    runtime: static
    buildCommand: npm install -g pnpm@9 && pnpm install && pnpm run build
    staticPublishPath: ./dist
    rootDir: ./frontend
    envVars:
      - key: NODE_VERSION
        value: "22"
      - key: PUBLIC_API_URL
        value: https://rut-verifier-api.onrender.com
